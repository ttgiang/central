<!--
	function cancelForm(){
		aseForm.action = "index.jsp";
		aseForm.submit();
	}

	function checkForm(){

		var user = document.aseForm.user.value;
		var passwd = document.aseForm.userpw.value;

		if ( user == "" ){
			alert("User ID is required");
			document.aseForm.user.focus();
			return false;
		}

		if ( passwd == "" ){
			alert("Password is required");
			document.aseForm.userpw.focus();
			return false;
		}

		return true;
	}
-->
